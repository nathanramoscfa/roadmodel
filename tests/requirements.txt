pytest>=8.0
pyyaml>=6.0
build>=1.2
# Cap below 2.0: mcp 2.0.0 removed the FastMCP class and the
# mcp.shared.memory.create_connected_server_and_client_session helper that
# src/roadmodel/mcp_server.py and tests/test_mcp_server.py target. Keep CI on
# the 1.x API until the 2.x MCPServer migration lands. Mirrors pyproject [mcp].
mcp>=1.0,<2
sqlparse>=0.4
# psycopg[binary] for tests/test_phase04_migrations.py — live-Postgres
# migration tests run under the migration-tests CI job against the
# infra/test-postgres/docker-compose.yml stack. Module-level skip
# when DATABASE_URL is unset, so plain `pytest tests/` without the
# Postgres up still passes.
psycopg[binary]>=3.1
